meta {
  name: CURRENT
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/banners/current
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
